// Title displayed in intro state
class Title extends LargeText {
  constructor() {
    super();
    // position
    this.x = width / 2;
    this.y = height / 3;
    // text information
    this.string = `Where's
  Sausage Dog?`;
  }
}
